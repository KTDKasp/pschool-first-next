import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Courses",
}

export default async function PageCoursesAll() {
  return (
    <div>
      All Courses
    </div>
  )
}