import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Courses",
}

export default async function TopPageAll() {
  return (
    <div>
      All Courses
    </div>
  )
}